{
  "collectionName": "components_landing_hero_audiences",
  "info": {
    "displayName": "Hero audience",
    "description": "App Developers vs Game Publishers hero block",
    "icon": "cursor"
  },
  "options": {},
  "attributes": {
    "audienceKey": {
      "type": "enumeration",
      "enum": [
        "apps",
        "games"
      ],
      "required": true
    },
    "title": {
      "type": "string",
      "description": "Plain headline when Title parts is empty. When Title parts has entries, this is optional (used as fallback for older clients)."
    },
    "titleParts": {
      "type": "component",
      "repeatable": true,
      "component": "landing.hero-title-part",
      "description": "Colored headline: add one row per phrase or word. Example: \"Welcome \" (default color), \"app\" (#2563eb), \" or \" (default), \"game\" (#dc2626)."
    },
    "description": {
      "type": "text",
      "required": true
    },
    "primaryCta": {
      "type": "string",
      "required": true
    },
    "otherCta": {
      "type": "string",
      "required": true
    },
    "values": {
      "type": "json",
      "required": true,
      "description": "Hero stats row: { \"years_experience\": \"10+\", \"budget_managed\": \"30M\", \"networks\": \"10+\" } (same keys for apps and games)."
    },
    "splitLeft": {
      "type": "component",
      "repeatable": false,
      "component": "landing.hero-split-column"
    },
    "splitRight": {
      "type": "component",
      "repeatable": false,
      "component": "landing.hero-split-column"
    }
  }
}