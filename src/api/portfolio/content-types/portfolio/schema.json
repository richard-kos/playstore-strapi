{
  "kind": "collectionType",
  "collectionName": "portfolios",
  "info": {
    "singularName": "portfolio",
    "pluralName": "portfolios",
    "displayName": "Portfolio",
    "description": "Portfolio work items (poster image, optional video, copy) for playstore-mvp /portfolio"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "developer": {
      "type": "string",
      "required": false
    },
    "text": {
      "type": "text",
      "required": false
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["images"]
    },
    "video": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["videos"]
    },
    "audienceKey": {
      "type": "enumeration",
      "enum": ["apps", "games"],
      "default": "games",
      "description": "Which /portfolio toggle audience this item shows under (App Developer = apps, Game Publisher = games). Mirrors Hero audience audienceKey."
    }
  }
}
